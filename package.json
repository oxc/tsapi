{
  "name": "@oxc/tsapi",
  "version": "0.0.1",
  "packageManager": "yarn@3.2.3",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -t -v run build"
  },
  "devDependencies": {
    "@yarnpkg/sdks": "^3.0.0-rc.7",
    "prettier": "2.6.2",
    "typescript": "^4.8.4"
  }
}
