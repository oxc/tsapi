{
  "name": "@oxc/tsapi",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/oxc/tsapi"
  },
  "packageManager": "yarn@3.5.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -t -v run build",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "prettier": "2.8.8",
    "typescript": "^5.0.4"
  }
}
